import React from "react";
import "./Questions.css";

const Questions = ({ mix }) => {
  return (
    <main className={`questions ${mix}`}>
      <h1>questions</h1>
    </main>
  );
};

export default Questions;
