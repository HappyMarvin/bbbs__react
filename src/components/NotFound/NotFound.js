import React from "react";
import "./NotFound.css";

const NotFound = ({ mix }) => {
  return (
    <main className={`not-found ${mix}`}>
      <h1>Not Found</h1>
    </main>
  );
};

export default NotFound;
