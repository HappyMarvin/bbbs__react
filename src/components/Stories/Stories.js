import React from "react";
import "./Stories.css";

const Stories = ({ mix }) => {
  return (
    <main className={`stories ${mix}`}>
      <h1>stories</h1>
    </main>
  );
};

export default Stories;
