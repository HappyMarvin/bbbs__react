import React from "react";
import "./Video.css";

const Video = ({ mix }) => {
  return (
    <main className={`video ${mix}`}>
      <h1>Read And Watch - Video</h1>
    </main>
  );
};

export default Video;
