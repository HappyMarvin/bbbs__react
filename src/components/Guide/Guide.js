import React from "react";
import "./Guide.css";

const Guide = ({ mix }) => {
  return (
    <main className={`guide ${mix}`}>
      <h1>Read And Watch - Guide</h1>
    </main>
  );
};

export default Guide;
